<link rel="stylesheet" type="text/css" href="forms.css">
<%= form_for(@requirement) do |f| %>
  <% if @requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requirement.errors.count, "error") %> prohibited this requirement from being saved:</h2>

      <ul>
      <% @requirement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class = "left">r
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name,:class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description,:class => 'form-control' %>
  </div>
</div>
  <div class="field">
    <%= f.label :event_id %><br>
    
    <%= f.collection_select :event_id, Event.all,  :id , :name , { :selected => Event.name }%> 
  </div>
  <div class="field">
    <%= f.label :rule_id %><br>
    
    <%= f.collection_select :rule_id, Rule.all,  :id , :name , { :selected => Rule.name }%> 
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary'%>
  </div>
<% end %>
